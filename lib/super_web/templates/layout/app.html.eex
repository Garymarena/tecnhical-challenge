<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8"/>
    <meta http-equiv="X-UA-Compatible" content="IE=edge"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <title>Super | Test</title>
    <link rel="stylesheet" href="<%= Routes.static_path(@conn, "/css/app.css") %>"/>
    <script defer type="text/javascript" src="<%= Routes.static_path(@conn, "/js/app.js") %>"></script>
  </head>
  <body class="text-gray-800 antialiased">
    <script>
      /*to prevent Firefox FOUC, this must be here*/
      let FF_FOUC_FIX;
    </script>
    <nav class="bg-white shadow">
      <div class="w-full container mx-auto flex flex-wrap items-center justify-between mt-0 px-6 py-3">
        <div class="flex justify-between items-center">
          <div>
            <img src="<%= Routes.static_path(@conn, "/images/logo.svg") %>"/>
          </div>
        </div>
    
        
    
        <div class="flex items-center">
          <div class="flex flex-col md:flex-row md:mx-6">
            <h1 class="my-4 text-3xl leading-none lg:text-5xl font-display text-super-blue-700">Seguros de Vida</h1>
        </div>
    
          <div class="flex justify-center md:block">
            <a class="relative text-gray-700 hover:text-gray-600" href="#">
                <img src="<%= Routes.static_path(@conn, "/images/menu-icon.svg") %>"/>
            </a>
          </div>
        </div>
      </div>
    </nav>
    <header></header>
    <main role="main" class="container">
      <p class="alert alert-info" role="alert"><%= get_flash(@conn, :info) %></p>
      <p class="alert alert-danger" role="alert"><%= get_flash(@conn, :error) %></p>
      <%= @inner_content %>
    </main>
  </body>
</html>
